import { Button, styled } from "tamagui";

export const CirlceButton = styled(Button, {
  borderRadius: "$12",
  padding: "$0",
  width: "$4",
  height: "$4",
  alignItems: "center",
  justifyContent: "center",
});
